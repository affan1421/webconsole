import { SecondaryClassSelectedPipe } from './secondary-class-selected.pipe';

describe('SecondaryClassSelectedPipe', () => {
  it('create an instance', () => {
    const pipe = new SecondaryClassSelectedPipe();
    expect(pipe).toBeTruthy();
  });
});
