import { BulkProfileUpdaterDirective } from './bulk-profile-updater.directive';

describe('BulkProfileUpdaterDirective', () => {
  it('should create an instance', () => {
    const directive = new BulkProfileUpdaterDirective();
    expect(directive).toBeTruthy();
  });
});
